---
title: Component Kullanımı
sidebar_position: 6
---

# Component Kullanımı

import { FireOceanLoading, LoadingCircle } from '@site/src/components/Loading';
import NoContentHere from '@site/src/components/MDX/NoContentHere';
import Image from '@site/src/components/MDX/Image';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import BrowserPreview from '@site/src/components/BrowserPreview';

## Tabs Component

```````jsx
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

```mdx-code-block
    <Tabs>
    <TabItem value="c" label="C Lang" default>
    ``````c
    #include <stdio.h>
    int main() {
      printf("Hello, World!");
      return 0;
    }
    ``````mdx-code-block
    </TabItem>
    <TabItem value="cpp" label="C++">
    ```;
```````

```mdx-code-block
<BrowserPreview>
```

```mdx-code-block
<Tabs>
<TabItem value="c" label="C Lang" default>
```

```c
#include <stdio.h>
int main() {
   printf("Hello, World!");
   return 0;
}
```

```mdx-code-block
</TabItem>
<TabItem value="cpp" label="C++">
```

```cpp
#include <iostream>

int main() {
    std::cout << "Hello World!";
    return 0;
}
```

```mdx-code-block
</TabItem>
<TabItem value="java" label="Java">
```

```java
class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```

```mdx-code-block
</TabItem>
</Tabs>
```

```mdx-code-block
</BrowserPreview>
```

## Loading Components

```js
import { FireOceanLoading, LoadingCircle } from '@site/src/components/Loading';

<FireOceanLoading />
<LoadingCircle />
```

```mdx-code-block
<BrowserPreview>
```

<FireOceanLoading />
<LoadingCircle />

```mdx-code-block
</BrowserPreview>
```

---

## Admonition MD Components

```js
:::note Başlık

Hello World!

:::
```

```mdx-code-block
<BrowserPreview>
```

:::note Note = Info

Lorem Ipsum In publishing and graphic design, Lorem ipsum is a placeholder text commonly used to demonstrate the visual form of a document or a typeface without relying on meaningful content.

:::

:::tip Tip = Success

Lorem Ipsum In publishing and graphic design, Lorem ipsum is a placeholder text commonly used to demonstrate the visual form of a document or a typeface without relying on meaningful content.

:::

:::info Info = Primary

Lorem Ipsum In publishing and graphic design, Lorem ipsum is a placeholder text commonly used to demonstrate the visual form of a document or a typeface without relying on meaningful content.

:::

:::caution Caution = Warning

Lorem Ipsum In publishing and graphic design, Lorem ipsum is a placeholder text commonly used to demonstrate the visual form of a document or a typeface without relying on meaningful content.

:::

:::danger Danger = Danger

Lorem Ipsum In publishing and graphic design, Lorem ipsum is a placeholder text commonly used to demonstrate the visual form of a document or a typeface without relying on meaningful content.

:::

```mdx-code-block
</BrowserPreview>
```

## Details Element Component

```jsx
<details>
  <summary>Toggle me!</summary>
  <div>
    <div>This is the detailed content</div>
    <br />
    <details>
      <summary>Nested toggle! Some surprise inside...</summary>
      <div>😲😲😲😲😲</div>
    </details>
  </div>
</details>
```

```mdx-code-block
<BrowserPreview>
```

<details>
  <summary>Toggle me!</summary>
  <div>
    <div>This is the detailed content</div>
    <br />
    <details>
      <summary>Nested toggle! Some surprise inside...</summary>
      <div>😲😲😲😲😲</div>
    </details>
  </div>
</details>

```mdx-code-block
</BrowserPreview>
```

## Image Component

```jsx
<Image copyRight="CopyRight">![Image Title](Image URL)</Image>
```

```mdx-code-block
<BrowserPreview>
```

<Image copyRight="Fire Ocean">

![Slash Introducing](https://user-images.githubusercontent.com/50423368/184932814-2b2c4b80-3530-42a1-8052-f1e922fb3868.png)

</Image>

```mdx-code-block
</BrowserPreview>
```

## Kaynaklar Bağlantısı

```md
:::info Kaynaklar Bağlantısı

<Tabs>
  <TabItem value="youtube-videos" label="Youtube videoları" default>
    <ul>
      <li>
        <a href="https://youtube.com">Video Title</a>
      </li>
    </ul>
  </TabItem>
  <TabItem value="blog-article" label="Blog & Makale">
    <ul>
      <li>
        <a href="https://medium.com">Blog Title</a>
      </li>
    </ul>
  </TabItem>
  <TabItem value="questions" label="Sorular">
    <ul>
      <li>
        <a href="https://hackerrank.com">Question Title</a>
      </li>
    </ul>
  </TabItem>
</Tabs>

:::
```

```mdx-code-block
<BrowserPreview>
```

:::info Kaynaklar Bağlantısı

<Tabs>
  <TabItem value="youtube-videos" label="Youtube videoları" default>
    <ul>
      <li>
        <a href="https://youtube.com">Video Title</a>
      </li>
    </ul>
  </TabItem>
  <TabItem value="blog-article" label="Blog & Makale">
    <ul>
      <li>
        <a href="https://medium.com">Blog Title</a>
      </li>
    </ul>
  </TabItem>
  <TabItem value="questions" label="Sorular">
    <ul>
      <li>
        <a href="https://hackerrank.com">Question Title</a>
      </li>
    </ul>
  </TabItem>
</Tabs>

:::

```mdx-code-block
</BrowserPreview>
```

## No Content Here Component

```jsx
import NoContentHere from '@site/src/components/MDX/NoContentHere';

<NoContentHere URL="https://github.com/Fire-Oceann/BB-TR-Kaynak" />;
```

```mdx-code-block
<BrowserPreview>
```

<NoContentHere URL="https://github.com/Fire-Oceann/BB-TR-Kaynak" />

```mdx-code-block
</BrowserPreview>
```

## Line highlighting

### Highlighting with comments

Hangi satırların vurgulanacağını seçmek için `vurgula-sonraki satır`, `vurgula-başla` ve `vurgula-bitiş` ile yorumları kullanabilirsiniz.

````md
```js
function HighlightSomeText(highlight) {
  if (highlight) {
    // highlight-next-line
    return 'This text is highlighted!';
  }

  return 'Nothing highlighted';
}

function HighlightMoreText(highlight) {
  // highlight-start
  if (highlight) {
    return 'This range is highlighted!';
  }
  // highlight-end

  return 'Nothing highlighted';
}
```
````

```mdx-code-block
<BrowserPreview>
```

```js
function HighlightSomeText(highlight) {
  if (highlight) {
    // highlight-next-line
    return 'This text is highlighted!';
  }

  return 'Nothing highlighted';
}

function HighlightMoreText(highlight) {
  // highlight-start
  if (highlight) {
    return 'This range is highlighted!';
  }
  // highlight-end

  return 'Nothing highlighted';
}
```

```mdx-code-block
</BrowserPreview>
```

Desteklenen yorum sözdizimi:

| Style      | Syntax                   |
| ---------- | ------------------------ |
| C-style    | `/* ... */` and `// ...` |
| JSX-style  | `{/* ... */}`            |
| Bash-style | `# ...`                  |
| HTML-style | `<!-- ... -->`           |

### Highlighting with metadata string

Ayrıca dil meta dizesinde vurgulanan satır aralıklarını da belirtebilirsiniz (dilden sonra bir boşluk bırakın). Birden çok satırı vurgulamak için satır numaralarını virgülle ayırın veya bir satır yığını seçmek için aralık sözdizimini kullanın. Bu özellik, ayrıştırma numarası aralığı kitaplığını kullanır ve proje ayrıntılarında daha fazla sözdizimi bulabilirsiniz.

````md
```jsx {1,4-6,11}
import React from 'react';

function MyComponent(props) {
  if (props.isBar) {
    return <div>Bar</div>;
  }

  return <div>Foo</div>;
}

export default MyComponent;
```
````

```mdx-code-block
<BrowserPreview>
```

```jsx {1,4-6,11}
import React from 'react';

function MyComponent(props) {
  if (props.isBar) {
    return <div>Bar</div>;
  }

  return <div>Foo</div>;
}

export default MyComponent;
```

```mdx-code-block
</BrowserPreview>
```

### Line numbering

Dil meta dizesinde `showLineNumbers` tuşunu kullanarak kod bloğunuz için satır numaralandırmayı etkinleştirebilirsiniz (anahtarın hemen önüne boşluk eklemeyi unutmayın).

````md
```jsx {1,4-6,11} showLineNumbers
import React from 'react';

function MyComponent(props) {
  if (props.isBar) {
    return <div>Bar</div>;
  }

  return <div>Foo</div>;
}

export default MyComponent;
```
````

```mdx-code-block
<BrowserPreview>
```

```jsx {1,4-6,11} showLineNumbers
import React from 'react';

function MyComponent(props) {
  if (props.isBar) {
    return <div>Bar</div>;
  }

  return <div>Foo</div>;
}

export default MyComponent;
```

```mdx-code-block
</BrowserPreview>
```

## Code Title

````md
```jsx title="/src/components/HelloCodeTitle.js"
function HelloCodeTitle(props) {
  return <h1>Hello, {props.name}</h1>;
}
```
````

```mdx-code-block
<BrowserPreview>
```

```jsx title="/src/components/HelloCodeTitle.js"
function HelloCodeTitle(props) {
  return <h1>Hello, {props.name}</h1>;
}
```

```mdx-code-block
</BrowserPreview>
```

## Live Code Editor

````md
```jsx live
function Clock(props) {
  const [date, setDate] = useState(new Date());
  useEffect(() => {
    const timerID = setInterval(() => tick(), 1000);

    return function cleanup() {
      clearInterval(timerID);
    };
  });

  function tick() {
    setDate(new Date());
  }

  return (
    <div>
      <h2>It is {date.toLocaleTimeString()}.</h2>
    </div>
  );
}
```
````

```mdx-code-block
<BrowserPreview>
```

```jsx live
function Clock(props) {
  const [date, setDate] = useState(new Date());
  useEffect(() => {
    const timerID = setInterval(() => tick(), 1000);

    return function cleanup() {
      clearInterval(timerID);
    };
  });

  function tick() {
    setDate(new Date());
  }

  return (
    <div>
      <h2>It is {date.toLocaleTimeString()}.</h2>
    </div>
  );
}
```

```mdx-code-block
</BrowserPreview>
```
